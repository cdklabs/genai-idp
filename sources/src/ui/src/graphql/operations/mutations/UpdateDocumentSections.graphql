mutation UpdateDocumentSections($objectKey: String!, $sections: [DocumentSectionGroupingInput!]!) {
  updateDocumentSections(objectKey: $objectKey, sections: $sections) {
    success
    message
    processingJobId
  }
}
