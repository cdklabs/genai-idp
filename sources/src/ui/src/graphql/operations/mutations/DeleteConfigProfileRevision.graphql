mutation DeleteConfigProfileRevision($profileName: String!, $revision: Int!) {
  deleteConfigProfileRevision(profileName: $profileName, revision: $revision) {
    success
    message
    error {
      type
      message
    }
  }
}
