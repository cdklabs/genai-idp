query GetDocumentCount($startDateTime: AWSDateTime, $endDateTime: AWSDateTime, $view: DocumentView) {
  getDocumentCount(startDateTime: $startDateTime, endDateTime: $endDateTime, view: $view) {
    count
  }
}
