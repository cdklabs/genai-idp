# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: release
on:
  push:
    branches:
      - main
  workflow_dispatch: {}
jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      latest_commit: ${{ steps.git_remote.outputs.latest_commit }}
      publish-cdklabs-genai-idp: ${{ steps.check-publish-cdklabs-genai-idp.outputs.publish }}
      publish-cdklabs-genai-idp-bda-processor: ${{ steps.check-publish-cdklabs-genai-idp-bda-processor.outputs.publish }}
      publish-cdklabs-genai-idp-bedrock-llm-processor: ${{ steps.check-publish-cdklabs-genai-idp-bedrock-llm-processor.outputs.publish }}
      publish-cdklabs-genai-idp-sagemaker-udop-processor: ${{ steps.check-publish-cdklabs-genai-idp-sagemaker-udop-processor.outputs.publish }}
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Set git identity
        run: |-
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
      - name: Install dependencies
        run: yarn install --check-files --frozen-lockfile
      - name: release
        run: npx projen release
      - id: check-publish-cdklabs-genai-idp
        run: (git ls-remote -q --exit-code --tags origin $(cat dist/releasetag.txt) && (echo "publish=false" >> $GITHUB_OUTPUT)) || echo "publish=true" >> $GITHUB_OUTPUT
        working-directory: packages/@cdklabs/genai-idp
      - id: check-publish-cdklabs-genai-idp-bda-processor
        run: (git ls-remote -q --exit-code --tags origin $(cat dist/releasetag.txt) && (echo "publish=false" >> $GITHUB_OUTPUT)) || echo "publish=true" >> $GITHUB_OUTPUT
        working-directory: packages/@cdklabs/genai-idp-bda-processor
      - id: check-publish-cdklabs-genai-idp-bedrock-llm-processor
        run: (git ls-remote -q --exit-code --tags origin $(cat dist/releasetag.txt) && (echo "publish=false" >> $GITHUB_OUTPUT)) || echo "publish=true" >> $GITHUB_OUTPUT
        working-directory: packages/@cdklabs/genai-idp-bedrock-llm-processor
      - id: check-publish-cdklabs-genai-idp-sagemaker-udop-processor
        run: (git ls-remote -q --exit-code --tags origin $(cat dist/releasetag.txt) && (echo "publish=false" >> $GITHUB_OUTPUT)) || echo "publish=true" >> $GITHUB_OUTPUT
        working-directory: packages/@cdklabs/genai-idp-sagemaker-udop-processor
      - name: Check for new commits
        id: git_remote
        run: echo "latest_commit=$(git ls-remote origin -h ${{ github.ref }} | cut -f1)" >> $GITHUB_OUTPUT
      - name: "@cdklabs/genai-idp: Backup artifact permissions"
        if: ${{ steps.git_remote.outputs.latest_commit == github.sha }}
        run: cd dist && getfacl -R . > permissions-backup.acl
        continue-on-error: true
        working-directory: packages/@cdklabs/genai-idp
      - name: "@cdklabs/genai-idp: Upload artifact"
        if: ${{ steps.git_remote.outputs.latest_commit == github.sha }}
        uses: actions/upload-artifact@v4.6.2
        with:
          name: cdklabs-genai-idp_build-artifact
          path: packages/@cdklabs/genai-idp/dist
          overwrite: true
      - name: "@cdklabs/genai-idp-bda-processor: Backup artifact permissions"
        if: ${{ steps.git_remote.outputs.latest_commit == github.sha }}
        run: cd dist && getfacl -R . > permissions-backup.acl
        continue-on-error: true
        working-directory: packages/@cdklabs/genai-idp-bda-processor
      - name: "@cdklabs/genai-idp-bda-processor: Upload artifact"
        if: ${{ steps.git_remote.outputs.latest_commit == github.sha }}
        uses: actions/upload-artifact@v4.6.2
        with:
          name: cdklabs-genai-idp-bda-processor_build-artifact
          path: packages/@cdklabs/genai-idp-bda-processor/dist
          overwrite: true
      - name: "@cdklabs/genai-idp-bedrock-llm-processor: Backup artifact permissions"
        if: ${{ steps.git_remote.outputs.latest_commit == github.sha }}
        run: cd dist && getfacl -R . > permissions-backup.acl
        continue-on-error: true
        working-directory: packages/@cdklabs/genai-idp-bedrock-llm-processor
      - name: "@cdklabs/genai-idp-bedrock-llm-processor: Upload artifact"
        if: ${{ steps.git_remote.outputs.latest_commit == github.sha }}
        uses: actions/upload-artifact@v4.6.2
        with:
          name: cdklabs-genai-idp-bedrock-llm-processor_build-artifact
          path: packages/@cdklabs/genai-idp-bedrock-llm-processor/dist
          overwrite: true
      - name: "@cdklabs/genai-idp-sagemaker-udop-processor: Backup artifact permissions"
        if: ${{ steps.git_remote.outputs.latest_commit == github.sha }}
        run: cd dist && getfacl -R . > permissions-backup.acl
        continue-on-error: true
        working-directory: packages/@cdklabs/genai-idp-sagemaker-udop-processor
      - name: "@cdklabs/genai-idp-sagemaker-udop-processor: Upload artifact"
        if: ${{ steps.git_remote.outputs.latest_commit == github.sha }}
        uses: actions/upload-artifact@v4.6.2
        with:
          name: cdklabs-genai-idp-sagemaker-udop-processor_build-artifact
          path: packages/@cdklabs/genai-idp-sagemaker-udop-processor/dist
          overwrite: true
  cdklabs-genai-idp_release_github:
    name: "@cdklabs/genai-idp: Publish to GitHub Releases"
    needs:
      - release
      - cdklabs-genai-idp_release_npm
      - cdklabs-genai-idp_release_pypi
      - cdklabs-genai-idp_release_nuget
    runs-on: ubuntu-latest
    permissions:
      contents: write
    if: ${{ needs.release.outputs.latest_commit == github.sha && needs.release.outputs.publish-cdklabs-genai-idp == 'true' }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: cdklabs-genai-idp_build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: errout=$(mktemp); gh release create $(cat dist/releasetag.txt) -R $GITHUB_REPOSITORY -F dist/changelog.md -t $(cat dist/releasetag.txt) --target $GITHUB_SHA 2> $errout && true; exitcode=$?; if [ $exitcode -ne 0 ] && ! grep -q "Release.tag_name already exists" $errout; then cat $errout; exit $exitcode; fi
  cdklabs-genai-idp_release_npm:
    name: "@cdklabs/genai-idp: Publish to npm"
    needs: release
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    if: ${{ needs.release.outputs.latest_commit == github.sha && needs.release.outputs.publish-cdklabs-genai-idp == 'true' }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: cdklabs-genai-idp_build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          NPM_DIST_TAG: latest
          NPM_REGISTRY: registry.npmjs.org
          NPM_CONFIG_PROVENANCE: "true"
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npx -p publib@latest publib-npm
  cdklabs-genai-idp_release_pypi:
    name: "@cdklabs/genai-idp: Publish to PyPI"
    needs: release
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: ${{ needs.release.outputs.latest_commit == github.sha && needs.release.outputs.publish-cdklabs-genai-idp == 'true' }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: cdklabs-genai-idp_build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          TWINE_USERNAME: ${{ secrets.TWINE_USERNAME }}
          TWINE_PASSWORD: ${{ secrets.TWINE_PASSWORD }}
        run: npx -p publib@latest publib-pypi
  cdklabs-genai-idp_release_nuget:
    name: "@cdklabs/genai-idp: Publish to NuGet Gallery"
    needs: release
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: ${{ needs.release.outputs.latest_commit == github.sha && needs.release.outputs.publish-cdklabs-genai-idp == 'true' }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 6.x
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: cdklabs-genai-idp_build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
        run: npx -p publib@latest publib-nuget
  cdklabs-genai-idp-bda-processor_release_github:
    name: "@cdklabs/genai-idp-bda-processor: Publish to GitHub Releases"
    needs:
      - release
      - cdklabs-genai-idp-bda-processor_release_npm
      - cdklabs-genai-idp-bda-processor_release_pypi
      - cdklabs-genai-idp-bda-processor_release_nuget
    runs-on: ubuntu-latest
    permissions:
      contents: write
    if: ${{ needs.release.outputs.latest_commit == github.sha && needs.release.outputs.publish-cdklabs-genai-idp-bda-processor == 'true' }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: cdklabs-genai-idp-bda-processor_build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: errout=$(mktemp); gh release create $(cat dist/releasetag.txt) -R $GITHUB_REPOSITORY -F dist/changelog.md -t $(cat dist/releasetag.txt) --target $GITHUB_SHA 2> $errout && true; exitcode=$?; if [ $exitcode -ne 0 ] && ! grep -q "Release.tag_name already exists" $errout; then cat $errout; exit $exitcode; fi
  cdklabs-genai-idp-bda-processor_release_npm:
    name: "@cdklabs/genai-idp-bda-processor: Publish to npm"
    needs: release
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    if: ${{ needs.release.outputs.latest_commit == github.sha && needs.release.outputs.publish-cdklabs-genai-idp-bda-processor == 'true' }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: cdklabs-genai-idp-bda-processor_build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          NPM_DIST_TAG: latest
          NPM_REGISTRY: registry.npmjs.org
          NPM_CONFIG_PROVENANCE: "true"
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npx -p publib@latest publib-npm
  cdklabs-genai-idp-bda-processor_release_pypi:
    name: "@cdklabs/genai-idp-bda-processor: Publish to PyPI"
    needs: release
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: ${{ needs.release.outputs.latest_commit == github.sha && needs.release.outputs.publish-cdklabs-genai-idp-bda-processor == 'true' }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: cdklabs-genai-idp-bda-processor_build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          TWINE_USERNAME: ${{ secrets.TWINE_USERNAME }}
          TWINE_PASSWORD: ${{ secrets.TWINE_PASSWORD }}
        run: npx -p publib@latest publib-pypi
  cdklabs-genai-idp-bda-processor_release_nuget:
    name: "@cdklabs/genai-idp-bda-processor: Publish to NuGet Gallery"
    needs: release
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: ${{ needs.release.outputs.latest_commit == github.sha && needs.release.outputs.publish-cdklabs-genai-idp-bda-processor == 'true' }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 6.x
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: cdklabs-genai-idp-bda-processor_build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
        run: npx -p publib@latest publib-nuget
  cdklabs-genai-idp-bedrock-llm-processor_release_github:
    name: "@cdklabs/genai-idp-bedrock-llm-processor: Publish to GitHub Releases"
    needs:
      - release
      - cdklabs-genai-idp-bedrock-llm-processor_release_npm
      - cdklabs-genai-idp-bedrock-llm-processor_release_pypi
      - cdklabs-genai-idp-bedrock-llm-processor_release_nuget
    runs-on: ubuntu-latest
    permissions:
      contents: write
    if: ${{ needs.release.outputs.latest_commit == github.sha && needs.release.outputs.publish-cdklabs-genai-idp-bedrock-llm-processor == 'true' }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: cdklabs-genai-idp-bedrock-llm-processor_build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: errout=$(mktemp); gh release create $(cat dist/releasetag.txt) -R $GITHUB_REPOSITORY -F dist/changelog.md -t $(cat dist/releasetag.txt) --target $GITHUB_SHA 2> $errout && true; exitcode=$?; if [ $exitcode -ne 0 ] && ! grep -q "Release.tag_name already exists" $errout; then cat $errout; exit $exitcode; fi
  cdklabs-genai-idp-bedrock-llm-processor_release_npm:
    name: "@cdklabs/genai-idp-bedrock-llm-processor: Publish to npm"
    needs: release
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    if: ${{ needs.release.outputs.latest_commit == github.sha && needs.release.outputs.publish-cdklabs-genai-idp-bedrock-llm-processor == 'true' }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: cdklabs-genai-idp-bedrock-llm-processor_build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          NPM_DIST_TAG: latest
          NPM_REGISTRY: registry.npmjs.org
          NPM_CONFIG_PROVENANCE: "true"
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npx -p publib@latest publib-npm
  cdklabs-genai-idp-bedrock-llm-processor_release_pypi:
    name: "@cdklabs/genai-idp-bedrock-llm-processor: Publish to PyPI"
    needs: release
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: ${{ needs.release.outputs.latest_commit == github.sha && needs.release.outputs.publish-cdklabs-genai-idp-bedrock-llm-processor == 'true' }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: cdklabs-genai-idp-bedrock-llm-processor_build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          TWINE_USERNAME: ${{ secrets.TWINE_USERNAME }}
          TWINE_PASSWORD: ${{ secrets.TWINE_PASSWORD }}
        run: npx -p publib@latest publib-pypi
  cdklabs-genai-idp-bedrock-llm-processor_release_nuget:
    name: "@cdklabs/genai-idp-bedrock-llm-processor: Publish to NuGet Gallery"
    needs: release
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: ${{ needs.release.outputs.latest_commit == github.sha && needs.release.outputs.publish-cdklabs-genai-idp-bedrock-llm-processor == 'true' }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 6.x
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: cdklabs-genai-idp-bedrock-llm-processor_build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
        run: npx -p publib@latest publib-nuget
  cdklabs-genai-idp-sagemaker-udop-processor_release_github:
    name: "@cdklabs/genai-idp-sagemaker-udop-processor: Publish to GitHub Releases"
    needs:
      - release
      - cdklabs-genai-idp-sagemaker-udop-processor_release_npm
      - cdklabs-genai-idp-sagemaker-udop-processor_release_pypi
      - cdklabs-genai-idp-sagemaker-udop-processor_release_nuget
    runs-on: ubuntu-latest
    permissions:
      contents: write
    if: ${{ needs.release.outputs.latest_commit == github.sha && needs.release.outputs.publish-cdklabs-genai-idp-sagemaker-udop-processor == 'true' }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: cdklabs-genai-idp-sagemaker-udop-processor_build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: errout=$(mktemp); gh release create $(cat dist/releasetag.txt) -R $GITHUB_REPOSITORY -F dist/changelog.md -t $(cat dist/releasetag.txt) --target $GITHUB_SHA 2> $errout && true; exitcode=$?; if [ $exitcode -ne 0 ] && ! grep -q "Release.tag_name already exists" $errout; then cat $errout; exit $exitcode; fi
  cdklabs-genai-idp-sagemaker-udop-processor_release_npm:
    name: "@cdklabs/genai-idp-sagemaker-udop-processor: Publish to npm"
    needs: release
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    if: ${{ needs.release.outputs.latest_commit == github.sha && needs.release.outputs.publish-cdklabs-genai-idp-sagemaker-udop-processor == 'true' }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: cdklabs-genai-idp-sagemaker-udop-processor_build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          NPM_DIST_TAG: latest
          NPM_REGISTRY: registry.npmjs.org
          NPM_CONFIG_PROVENANCE: "true"
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npx -p publib@latest publib-npm
  cdklabs-genai-idp-sagemaker-udop-processor_release_pypi:
    name: "@cdklabs/genai-idp-sagemaker-udop-processor: Publish to PyPI"
    needs: release
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: ${{ needs.release.outputs.latest_commit == github.sha && needs.release.outputs.publish-cdklabs-genai-idp-sagemaker-udop-processor == 'true' }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: cdklabs-genai-idp-sagemaker-udop-processor_build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          TWINE_USERNAME: ${{ secrets.TWINE_USERNAME }}
          TWINE_PASSWORD: ${{ secrets.TWINE_PASSWORD }}
        run: npx -p publib@latest publib-pypi
  cdklabs-genai-idp-sagemaker-udop-processor_release_nuget:
    name: "@cdklabs/genai-idp-sagemaker-udop-processor: Publish to NuGet Gallery"
    needs: release
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: ${{ needs.release.outputs.latest_commit == github.sha && needs.release.outputs.publish-cdklabs-genai-idp-sagemaker-udop-processor == 'true' }}
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 6.x
      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: cdklabs-genai-idp-sagemaker-udop-processor_build-artifact
          path: dist
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
        run: npx -p publib@latest publib-nuget
